# rdp-io - Руководство пользователя

**Версия:** 1.0
**Дата:** 2025-12-29

---

## 📖 Содержание

1. [Введение](#введение)
2. [Назначение](#назначение)
3. [Установка и запуск](#установка-и-запуск)
4. [Быстрый старт](#быстрый-старт)
5. [Основные функции](#основные-функции)
6. [Настройки](#настройки)
7. [Troubleshooting](#troubleshooting)
8. [FAQ](#faq)

---

## Введение

**rdp-io** - это утилита для передачи текста в изолированные RDP-сессии через эмуляцию клавиатурного ввода.

### Ключевые особенности

✅ **Поддержка ASCII + кириллица** (EN/RU раскладки)
✅ **3 режима скорости** передачи (Fast/Reliable/Slow)
✅ **Работа через System Tray** - минимальный UI, не отвлекает
✅ **Обратный отсчет** перед передачей (время для переключения окна)
✅ **Автоматическая смена раскладок** для смешанного текста
✅ **Portable** - настройки и логи рядом с .exe

---

## Назначение

### Проблема

При работе с Remote Desktop (RDP) стандартный буфер обмена часто отключен по политикам безопасности. Это делает невозможным быстрое копирование:
- Длинных SQL запросов
- Логов и сообщений об ошибках
- Команд PowerShell/Bash
- Конфигурационных файлов

### Решение

rdp-io эмулирует **посимвольный** ввод текста через Win32 API `SendInput`, что воспринимается системой как реальная работа клавиатуры.

**Как это работает:**

1. Вы копируете текст в буфер обмена на **локальной машине**
2. Запускаете rdp-io → появляется обратный отсчет
3. Переключаетесь в RDP окно и ставите курсор в нужное место
4. Приложение автоматически "печатает" текст в активное окно

---

## Установка и запуск

### Системные требования

- **ОС:** Windows 7/10/11 (x64)
- **.NET Runtime:** .NET 10.0 или выше
- **Права:** Обычный пользователь (Admin не требуется)

### Установка

1. **Скачайте** архив с приложением
2. **Распакуйте** в любую директорию (например, `C:\Tools\rdp-io\`)
3. **Запустите** `RdpIo.App.exe`

При первом запуске будут созданы:
- `settings.json` - файл настроек
- `logs/` - директория с логами

### Первый запуск

1. Запустите `RdpIo.App.exe`
2. В System Tray (правый нижний угол экрана) появится иконка приложения
3. Наведите курсор - увидите tooltip с информацией о буфере обмена
4. Готово к использованию!

---

## Быстрый старт

### Пример 1: Передача SQL запроса

1. **Скопируйте** SQL запрос на локальной машине:
   ```sql
   SELECT * FROM users WHERE created_at > '2025-01-01';
   ```

2. **Правый клик** по иконке rdp-io в System Tray
   - (Или левый клик → "▶ Запустить передачу")

3. **Обратный отсчет** 5...4...3...2...1

4. Быстро **переключитесь** в RDP окно

5. **Кликните** в поле SQL редактора (где нужен запрос)

6. Текст автоматически **"печатается"** посимвольно!

### Пример 2: Передача смешанного текста (EN + RU)

```sql
SELECT * FROM таблица_заказов WHERE статус = 'новый';
```

rdp-io **автоматически** переключает раскладку при переходе с латиницы на кириллицу!

---

## Основные функции

### 1. Контекстное меню System Tray

**Правый клик** по иконке в трее открывает меню:

```
┌─────────────────────────────┐
│ ▶ Запустить передачу        │ ← Главная функция (жирный шрифт)
├─────────────────────────────┤
│ ⚙ Настройки                 │ ← Открывает окно настроек
│ ℹ О программе               │ ← Информация о версии
├─────────────────────────────┤
│ ✖ Выход                     │ ← Завершить приложение
└─────────────────────────────┘
```

### 2. Окно обратного отсчета

После запуска передачи появляется окно:

```
╔═══════════════════════════════╗
║  Подготовка к передаче        ║
║                               ║
║           3                   ║  ← Цифра уменьшается каждую секунду
║                               ║
║    [Отмена (Esc)]            ║
╚═══════════════════════════════╝
```

- Окно всегда **поверх всех окон** (Always-on-top)
- На последней секунде показывает **"START!"** зеленым цветом
- Можно отменить нажатием **Esc**

### 3. Окно прогресса передачи

Во время передачи отображается:

```
╔══════════════════════════════════════════╗
║  Передача текста                         ║
║                                          ║
║  [▓▓▓▓▓▓▓▓▓▓░░░░░░░░] 50%              ║
║                                          ║
║  Передано: 250 / 500 символов            ║
║  Скорость: 20 символов/сек               ║
║  Осталось: ~12 сек                       ║
║                                          ║
║    [Приостановить]  [Отмена (Esc)]      ║
╚══════════════════════════════════════════╝
```

**Возможности:**
- **Приостановить** - пауза передачи (можно продолжить)
- **Esc** - полная отмена передачи

### 4. Окно настроек

Открывается через меню "⚙ Настройки":

#### Секция: Передача данных

- **Режим передачи:**
  - **Fast** (20 мс между символами) - для надежных соединений
  - **Reliable** (50 мс) - рекомендуемый режим по умолчанию
  - **Slow** (100 мс) - для медленных RDP сессий

- **Время обратного отсчета:** 1-60 секунд (по умолчанию 5)

#### Секция: Звуковые уведомления

- ✓ Включить звуки
  - ✓ Звук при старте передачи
  - ✓ Звук при завершении
  - ✓ Звук при ошибке

#### Секция: Расширенные настройки

- **Время жизни кэша буфера:** 1-60 сек (по умолчанию 5)
- **Уровень логирования:** Trace/Debug/Info/Warning/Error
- **Максимальный размер лога:** 1-100 MB (по умолчанию 10)

---

## Настройки

### Файл settings.json

Все настройки хранятся в `settings.json` (рядом с .exe):

```json
{
  "TransmissionMode": "Reliable",
  "CountdownSeconds": 5,
  "EnableSounds": false,
  "SoundOnStart": true,
  "SoundOnComplete": true,
  "SoundOnError": true,
  "ClipboardCacheLifetimeSeconds": 5,
  "LogLevel": "Info",
  "MaxLogFileSizeMB": 10
}
```

Можно редактировать вручную (приложение перезагрузит настройки).

### Режимы передачи

| Режим | Задержка | Скорость | Рекомендуется |
|-------|----------|----------|---------------|
| **Fast** | 20 мс | ~50 символов/сек | Локальная сеть, быстрый RDP |
| **Reliable** | 50 мс | ~20 символов/сек | **Стандарт** (по умолчанию) |
| **Slow** | 100 мс | ~10 символов/сек | Медленный интернет, VPN |

**Совет:** Если при передаче символы "теряются" или появляются не все - переключитесь на **Slow**.

---

## Troubleshooting

### Проблема 1: Пропускаются символы

**Симптомы:** Текст передается, но некоторые буквы отсутствуют.

**Решение:**
1. Откройте **Настройки** (⚙)
2. Измените режим на **Slow** (100 мс)
3. Попробуйте снова

**Причина:** RDP сессия не успевает обрабатывать быстрый ввод.

---

### Проблема 2: Кириллица отображается как "????"

**Симптомы:** Русские буквы превращаются в знаки вопроса.

**Решение:**
1. Проверьте, что в RDP окне **установлена русская раскладка** (EN/RU)
2. Переключитесь вручную на русскую раскладку перед передачей
3. rdp-io автоматически переключится на нужную раскладку

**Причина:** Приложение эмулирует Alt+Shift внутренне, но базовая раскладка должна быть установлена.

---

### Проблема 3: Приложение не запускается

**Симптомы:** Двойной клик по .exe - ничего не происходит.

**Решение:**
1. Убедитесь, что установлен **.NET 10 Runtime**
   - Скачать: https://dotnet.microsoft.com/download/dotnet/10.0
2. Проверьте логи в `logs/app.log`
3. Если файл отсутствует - права на запись в директорию

---

### Проблема 4: "Ошибка валидации буфера обмена"

**Симптомы:** Уведомление "Буфер обмена пуст или недоступен".

**Решение:**
1. Скопируйте текст еще раз (Ctrl+C)
2. Подождите 1-2 секунды
3. Попробуйте снова

**Причина:** Буфер обмена занят другим приложением.

---

### Проблема 5: Текст вводится не в то окно

**Симптомы:** Текст появляется в неправильном месте.

**Решение:**
1. После запуска передачи (правый клик по иконке)
2. **Во время обратного отсчета** (5...4...3...)
3. Переключитесь в нужное окно
4. Кликните мышью в поле ввода текста
5. Дождитесь "START!" и не трогайте мышь/клавиатуру

**Совет:** Используйте Alt+Tab для быстрого переключения между окнами.

---

## FAQ

### ❓ Можно ли передавать пароли?

**Ответ:** Технически - да, но **не рекомендуется**!

Причины:
- Текст передается **посимвольно** и виден на экране
- Пароли сохраняются в буфере обмена (небезопасно)
- Логи могут содержать длину текста

Для паролей лучше использовать менеджеры паролей с поддержкой RDP.

---

### ❓ Поддерживаются ли другие языки (кроме EN/RU)?

**Текущая версия:** Только **английский (ASCII)** и **русский (кириллица)**.

**Будущие версии:** Планируется добавить:
- Украинский
- Немецкий
- Французский
- Испанский

---

### ❓ Можно ли передавать длинные тексты (1000+ символов)?

**Ответ:** Да! Ограничение - **100,000 символов** (задано в настройках валидации).

**Примерное время:**
- Fast (20 мс): 1000 символов = ~20 секунд
- Reliable (50 мс): 1000 символов = ~50 секунд
- Slow (100 мс): 1000 символов = ~100 секунд

**Совет:** Для текстов > 5000 символов рассмотрите альтернативы (shared folder, email).

---

### ❓ Работает ли приложение с VMware/VirtualBox?

**Ответ:** Да, но с ограничениями:
- **VMware:** Работает отлично (если отключен shared clipboard)
- **VirtualBox:** Работает (убедитесь, что Guest Additions не перехватывают ввод)
- **Hyper-V:** Работает

**Общее правило:** Если в виртуальной машине работает обычная клавиатура - rdp-io тоже будет работать.

---

### ❓ Можно ли запускать несколько экземпляров?

**Ответ:** Нет, допустим только **один экземпляр** одновременно.

При попытке запустить второй - первый экземпляр выйдет на передний план.

---

### ❓ Как удалить приложение?

**Ответ:** Просто удалите директорию с приложением.

Файлы, которые будут удалены:
- `RdpIo.App.exe` и все .dll
- `settings.json`
- `logs/app.log`

**Никаких записей в реестре не создается!**

---

### ❓ Приложение бесплатное?

**Ответ:** Да, полностью **бесплатное** и **открытое** (open-source).

Лицензия: MIT
Репозиторий: [GitHub ссылка]

---

### ❓ Как сообщить об ошибке?

**Ответ:**

1. Проверьте логи в `logs/app.log`
2. Создайте issue на GitHub с:
   - Описанием проблемы
   - Шагами для воспроизведения
   - Содержимым `app.log` (последние 50 строк)
   - Версией ОС и .NET Runtime

---

## 📞 Контакты и поддержка

- **GitHub Issues:** [ссылка на репозиторий]
- **Email:** support@textsimulator.com
- **Документация:** README.md, BUILD.md

---

## 📝 История версий

### v1.0.0 (2025-12-29)

**Первый релиз!**

Реализовано:
- ✅ Передача ASCII + кириллица (EN/RU)
- ✅ 3 режима скорости (Fast/Reliable/Slow)
- ✅ System Tray интерфейс
- ✅ Окно обратного отсчета (5 сек)
- ✅ Окно прогресса передачи
- ✅ Окно настроек (WPF)
- ✅ Portable deployment
- ✅ Логирование (файловое с ротацией)
- ✅ JSON настройки

---

**Спасибо за использование rdp-io!** 🎉

