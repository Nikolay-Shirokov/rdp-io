<Window x:Class="RdpIo.UI.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RdpIo.UI.Windows"
        xmlns:config="clr-namespace:RdpIo.Configuration;assembly=RdpIo.Configuration"
        xmlns:logging="clr-namespace:RdpIo.Infrastructure.Logging;assembly=RdpIo.Infrastructure"
        Title="Настройки rdp-io"
        Width="600" Height="650"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        WindowStartupLocation="CenterScreen">

    <Border Background="#2C3E50"
            BorderBrush="#3498DB"
            BorderThickness="3"
            CornerRadius="10">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0"
                       Text="⚙ Настройки"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,20"/>

            <!-- Содержимое -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Margin="0,0,0,20">
                <StackPanel>

                    <!-- Секция: Передача данных -->
                    <TextBlock Text="Передача данных"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#3498DB"
                               Margin="0,0,0,10"/>

                    <Border Background="#34495E"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <!-- Режим передачи -->
                            <TextBlock Text="Режим передачи:"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding TransmissionModes}"
                                      SelectedItem="{Binding SelectedTransmissionMode}"
                                      Height="35"
                                      FontSize="14"
                                      Margin="0,0,0,15"/>

                            <!-- Время обратного отсчета -->
                            <TextBlock Text="Время обратного отсчета (сек):"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Slider Width="400"
                                        Minimum="1"
                                        Maximum="60"
                                        Value="{Binding CountdownSeconds}"
                                        TickFrequency="1"
                                        IsSnapToTickEnabled="True"
                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding CountdownSeconds}"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Width="40"
                                           TextAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Секция: Звуковые уведомления -->
                    <TextBlock Text="Звуковые уведомления"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#3498DB"
                               Margin="0,0,0,10"/>

                    <Border Background="#34495E"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <CheckBox Content="Включить звуки"
                                      IsChecked="{Binding EnableSounds}"
                                      Foreground="White"
                                      FontSize="14"
                                      Margin="0,0,0,10"/>

                            <CheckBox Content="Звук при старте передачи"
                                      IsChecked="{Binding SoundOnStart}"
                                      IsEnabled="{Binding EnableSounds}"
                                      Foreground="White"
                                      FontSize="13"
                                      Margin="20,0,0,5"/>

                            <CheckBox Content="Звук при завершении"
                                      IsChecked="{Binding SoundOnComplete}"
                                      IsEnabled="{Binding EnableSounds}"
                                      Foreground="White"
                                      FontSize="13"
                                      Margin="20,0,0,5"/>

                            <CheckBox Content="Звук при ошибке"
                                      IsChecked="{Binding SoundOnError}"
                                      IsEnabled="{Binding EnableSounds}"
                                      Foreground="White"
                                      FontSize="13"
                                      Margin="20,0,0,5"/>
                        </StackPanel>
                    </Border>

                    <!-- Секция: Расширенные настройки -->
                    <TextBlock Text="Расширенные настройки"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#3498DB"
                               Margin="0,0,0,10"/>

                    <Border Background="#34495E"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <!-- Кэш буфера обмена -->
                            <TextBlock Text="Время жизни кэша буфера (сек):"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <Slider Width="400"
                                        Minimum="1"
                                        Maximum="60"
                                        Value="{Binding ClipboardCacheLifetime}"
                                        TickFrequency="1"
                                        IsSnapToTickEnabled="True"
                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ClipboardCacheLifetime}"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Width="40"
                                           TextAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>
                            </StackPanel>

                            <!-- Уровень логирования -->
                            <TextBlock Text="Уровень логирования:"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding LogLevels}"
                                      SelectedItem="{Binding SelectedLogLevel}"
                                      Height="35"
                                      FontSize="14"
                                      Margin="0,0,0,15"/>

                            <!-- Размер лога -->
                            <TextBlock Text="Максимальный размер лога (MB):"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Slider Width="400"
                                        Minimum="1"
                                        Maximum="100"
                                        Value="{Binding MaxLogFileSizeMB}"
                                        TickFrequency="1"
                                        IsSnapToTickEnabled="True"
                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding MaxLogFileSizeMB}"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Width="40"
                                           TextAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Секция: OCR настройки -->
                    <TextBlock Text="OCR настройки"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#3498DB"
                               Margin="0,0,0,10"/>

                    <Border Background="#34495E"
                            CornerRadius="5"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <!-- Язык распознавания -->
                            <TextBlock Text="Язык распознавания:"
                                       Foreground="White"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding OcrLanguages}"
                                      SelectedItem="{Binding SelectedOcrLanguage}"
                                      Height="35"
                                      FontSize="14"
                                      Margin="0,0,0,15"/>

                            <!-- Предобработка изображения -->
                            <CheckBox Content="Включить предобработку изображения для OCR"
                                      IsChecked="{Binding OcrEnablePreprocessing}"
                                      Foreground="White"
                                      FontSize="14"
                                      Margin="0,0,0,5"/>

                            <TextBlock Text="(Улучшает качество распознавания: серый градиент, контраст, снижение шума)"
                                       Foreground="#95A5A6"
                                       FontSize="11"
                                       Margin="20,0,0,5"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0">
                <Button Content="💾 Сохранить"
                        Width="150"
                        Height="45"
                        Margin="0,0,10,0"
                        Command="{Binding SaveCommand}"
                        Background="#27AE60"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2ECC71"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#95A5A6"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="❌ Отмена"
                        Width="150"
                        Height="45"
                        Command="{Binding CancelCommand}"
                        Background="#E74C3C"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C0392B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</Window>


